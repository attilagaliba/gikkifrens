query ($id: ID = "") {
  account(id: $id) {
    id
    poolMemberships(where: {isConnected: true}) {
      pool {
        flowRate
        admin {
          id
        }
        adjustmentFlowRate
        totalBuffer
        perUnitFlowRate
        perUnitSettledValue
        updatedAtTimestamp
        totalUnits
        totalConnectedUnits
      }
      units
      totalAmountClaimed
      syncedPerUnitSettledValue
      syncedPerUnitFlowRate
    }
  }
}